/**
 * Panels - Settings and Configuration UI Components
 * 
 * These panels provide user-facing settings interfaces for:
 * - Sidebar preferences (layout, behavior, display)
 * - Accessibility settings (motion, contrast, keyboard)
 * - Custom themes (colors, typography, effects) - Premium
 */

export { PreferencePanel } from './preference-panel'
export { AccessibilityPanel, AccessibilityTrigger } from './accessibility-panel'
export { CustomThemePanel } from './custom-theme'
